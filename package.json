{
  "name": "cinacina",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "node --check server.mjs && node --check lib.mjs && node --check dist/app.js && node --check dist/core.js && node --check dist/importer.js && node --check dist/import-worker.js && node --check dist/storage.js"
  },
  "engines": {
    "node": ">=24"
  },
  "dependencies": {
    "sql.js": "1.14.1"
  }
}
